const text = 'string do texro';

const size = 'texto'.length;

// Retorna um array quebrando pelo delimitador
const splittedText = 'Texto'.split('x');
// te, to

const replace = 'texto'.replace('Text', 'Txt');

// replace com regex
const replaceRegex = 'texto'.replace(/\n/g, 'Txt');

// Retorna um espaço da string, -1 pega o último caractere
const lastChar = 'texto'.slice(-1);

// Retorna do começo ao fim, menos o último caractere
// Slice passa o começo e o fim
const FullWithoutLastChar = 'texto'.slice(0, -1);
// text

const secoundToEnd = 'texto'.slice(1);
// exto

// Começo e a quantidade de caracteres
const subStr = 'texto'.substr(0, 2);


